// Package lifecycle defines the typed facts, stream values, drafts and outcomes
// exchanged between the Exchange state machine and directive modules. It does
// not own state transitions or execute handlers; Exchange remains the lifecycle
// owner.
package lifecycle
